
namespace CompGeo.Library.DataStructures
{
    /// <summary>
    /// Used for heap data structure.
    /// Specifies if the heap will be a min heap or a max heap.
    /// Min: smallest valued item on top of heap.
    /// Max: largest valued item on top of heap.
    /// </summary>
    public enum MinMax
    {
        Min,
        Max
    }
}
